export function stripRemasterAnnotations(songName: string): string {
  return songName
    .replace(/-? ?\(?(?:\d{4})? ?Remaster(?:ed)? ?(?:\d{4})?\)?/i, "")
    .replace(/-? ?\(?(?:\d{4})? ?Mix ?(?:\d{4})?\)?/i, "")
    .replace(/-? ?\(?(?:\d{4})? ?Stereo ?(?:\d{4})?\)?/i, "")
    .trim();
}
